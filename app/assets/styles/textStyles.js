import StyleConfig from '../../config/StyleConfig.js';

const textStyles = {
    boldText: {
        fontFamily: StyleConfig.BOLD_FONT,
    },
    regularText: {
        fontFamily: StyleConfig.REGULAR_FONT,
    },
    lightText: {
        fontFamily: StyleConfig.LIGHT_FONT,
    },
    mediumText: {
        fontFamily: StyleConfig.REGULAR_FONT,
        //fontFamily: StyleConfig.MEDIUM_FONT,
    },
};

export default textStyles;
